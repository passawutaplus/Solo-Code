import { AnnouncementBanner } from "@/components/AnnouncementBanner";

/** Home tab announcement slot — reuses global announcement carousel. */
export function HomeAnnouncementBanner() {
  return <AnnouncementBanner />;
}
